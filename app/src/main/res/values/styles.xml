<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ActionBar.Black</item>
    </style>

    <!-- Settings activity action bar styles -->
    <style name="ActionBar.Black" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/black</item>
        <item name="android:height">56dp</item>
    </style>

    <style name="ArtistsListStyle">
        <!-- Just a stub, will be overwritten for bigger screens -->
    </style>

</resources>
